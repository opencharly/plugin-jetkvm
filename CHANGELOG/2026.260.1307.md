# 2026.260.1307 — test(beds): add the read-only method-surface bed

## Summary of changes

Adds `jetkvm-device-methods`, a disposable **read-only** bed that exercises one representative method from every read-only family against the live device, plus the matcher-syntax fix the first run surfaced.

- `charly.yml`: the new bed, with 15 steps — `status`, `version`, `video-state`, `usb-state`, `atx-state`, `dc-state`, `virtual-media-state`, `storage-files`, `keyboard-layout`, `network-state`, `network-settings`, `tailscale-status`, `extensions`, `diagnostics`, and the raw `rpc` escape hatch. Nothing here mutates the appliance.
- One authoring defect found by the first run and fixed in the same change (R1): the matchers were written as the literal string `["contains: X"]` instead of the matcher list `[{contains: X}]`. All 15 steps failed on that, while still proving every method dispatched — each returned real device data. The corrected form passes.

**Closes / relates to:** `none`.

**Breaking change + rollback:** `none` — a test bed is added; nothing public changes. Revert the merge to roll back.

## Change class

`candy/box-config` — a `charly.yml` bed addition; no Go change.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** `charly box validate` plus `charly check run <bed>` on each disposable bed.

- **Eval beds run — the EXACT beds + per-bed results:**

  | bed | substrate | verdict | steps | total_seconds |
  |---|---|---|---|---|
  | `jetkvm-verb-probe` | local | ok | 6 | 16 |
  | `jetkvm-device-readonly` | local (live device) | ok | 6 | 29 |
  | `jetkvm-device-methods` | local (live device) | ok | 6 | 31 |

  All ran on `charly version` **2026.258.0220**; the new bed's run is `.check/jetkvm-device-methods/2026.260.1252/`.

- **Fresh rebuild:** `charly box validate` returns `OK — checked 1 candy, 3 deploys, 7 distros, 7 builders; 0 warnings, 0 errors`.

- **Did the CHANGED code path execute live?** Yes — the new bed's `check-live` against the live device:

  ```
  Local deploy: jetkvm-device-methods [host (local)]
    PASS  ... [jk-status]  jetkvm:    ok
  device_id: 8bf1cddc3ba45e77
  firmware:  0.5.9
  rpc:       true
    PASS  ... [jk-version]  {"appVersion":"0.5.9","systemVersion":"0.2.8"}
    PASS  ... [jk-video-state]  {"error":"no_signal","fps":0,"height":0,"ready":false,...}
    PASS  ... [jk-usb-state]  "configured"
    PASS  ... [jk-atx-state]  {"hdd":false,"power":false}
    PASS  ... [jk-dc-state]  {"current":0,"isOn":false,"power":0,"restoreState":0,"voltage":0}
    PASS  ... [jk-vm-state]  null
    PASS  ... [jk-storage]  {"files":[]}
    PASS  ... [jk-layout]  "en-US"
    PASS  ... [jk-net-state]  {"dhcp_lease":{...}}
    PASS  ... [jk-net-settings]  {"dhcp_client":"udhcpc",...}
    PASS  ... [jk-tailscale]  {"backendState":"Running",...}
    PASS  ... [jk-extensions]  "atx-power" / supported: atx-power, dc-power
    PASS  ... [jk-diagnostics]  device_id: 8bf1cddc3ba45e77 ...
    PASS  ... [jk-rpc]  "pong"

  15 steps: 15 passed, 0 failed, 0 skipped
  ```

- **Concurrency (shared-state changes only):** `N/A` — a bed addition; no shared state touched.

**Evidence — put each item in the block whose label names it.**

*1. Fresh-rebuild gate output*:
```
$ charly box validate
charly box validate: OK — checked 1 candy, 3 deploys, 7 distros, 7 builders; 0 warnings, 0 errors

$ charly check run jetkvm-verb-probe        -> PASS (steps=6)
$ charly check run jetkvm-device-readonly   -> PASS (steps=6)
$ charly check run jetkvm-device-methods    -> PASS (steps=6)
```

*2. Each changed code path executing*: the `charly.yml` bed is the entire change; its 15 steps ran live — pasted above.

*3. Docs-only evidence*: `N/A — not a docs-only change`.

## Attribution tier

`fully tested and validated` — the new bed ran live against the device at zero warnings.

## Harness rulebook compliance

- **R0 skills:** `/charly-check:check` (+ `beds-and-r10.md`, `verb-catalog.md`), `/charly-internals:local-infra`, `/charly-internals:git-workflow`.
- **R1 RCA + zero warnings:** the first run's 15 failures were root-caused to one matcher-syntax error (literal string vs matcher list) and fixed; zero non-allowlisted warnings.
- **R2 no out-of-scope:** the defect surfaced by this change's own first run is fixed here.
- **R3 no duplication:** the new bed reuses the existing two beds' shape and the shared matcher vocabulary.
- **R4 no workaround:** no retry or suppression; the matcher form was corrected.
- **R5 hard cutover + grep:** `N/A — purely additive, no identifier removed or renamed`.
- **R6 git safety:** status checked; no force update, no hooks bypass, no direct push to the default branch.
- **R7 runtime gate:** the new bed is the runtime gate for its own content, and it ran live.
- **R8 artifact invariants:** `N/A — no generated artifact`.
- **R9 binary == source + deps:** the plugin is host-built by charly's loader from the branch source; no new OS dependency.
- **R10 disposable + coverage:** the bed is `disposable: true` and read-only.
- **RDD / ADE / SDD:** ADE — the candy's plan is unchanged and still ships a deterministic build-context check plus the runtime dispatch step; SDD — no schema change.
- **Kernel/plugin boundary law:** no core or `sdk` change.
- **Disposable-only autonomy:** only `disposable: true` beds were destroyed/rebuilt; the device was touched read-only.
- **Clean architecture + Go gates:** no Go change in this PR.
- **CHANGELOG:** `N/A — this repo keeps no CHANGELOG/`; `tag-on-merge` generates it from this body.

---

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*